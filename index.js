import React from "react";
import ReactDOM from "react-dom/client";
import OrionStore from OrionStore 
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <OrionStore />
  </React.StrictMode>
);
